export default function Presentacion(props) {
    return (
      <div>
        <p>Nombre: {props.nombre}</p>
        <p>Edad: {props.edad}</p>
        <p>Profesión: {props.profesion}</p>
      </div>
    );
  }
  